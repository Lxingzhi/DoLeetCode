package com.lys

/**
 * Created:2019-08-24 10: 06、
 * Email:liu.yanshan@aldelo.com
 * @author :liuyanshan
 * 描    述：
 */


/**
 * 运算符相关
 * 与运算（&）：两个都为1结果才为1，否则为0；
 * 或运算（|）：两个中的其中一个为1 就为1，两个都为0才为0
 * 异或（^）：相同为数字不同就为1，相同就0；
 *
 * LinkedList 和arrayList 却别
 * linkList 是链表式，增删 快。不需要重新排列列表直接插入或者删除即可。但是查找需要查到链表的前键或者后健
 * arrayList 查找快 因为是数组形式，通过角标直接获取数据。如果增删的话数组需要重新排列所以慢
 *
 *
 */